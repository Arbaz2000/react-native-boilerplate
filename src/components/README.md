# src/components

### One Job
Presentational design system components with zero business logic:
- `ui/`: Buttons, Inputs, Cards (consume theme tokens only)
- `layout/`: SafeScreen, KeyboardAvoidingWrapper, TabBarIcon
- `feedback/`: Toast, Skeleton, ErrorBoundary
