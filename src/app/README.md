# src/app

### One Job
Application-level composition root. Houses `App.tsx`, `AppNavigator.tsx` (top-level routing & cold-start gating), the unified `<AppProviders>` tree, and bootstrap version checks.
