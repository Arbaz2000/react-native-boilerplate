# src/store

### One Job
Global client and server state management:
- Redux Toolkit + RTK Query store and root reducer
- Zustand stores for ephemeral UI state (toasts, loaders, sheets, onboarding status, theme selection)
