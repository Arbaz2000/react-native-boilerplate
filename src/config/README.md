# src/config

### One Job
Static runtime configuration, environment variable bindings (`env.ts`), centralized branding identities (`branding.ts`), and swappable bottom tab registry (`tabs.config.ts`).
